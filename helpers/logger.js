// const fs = require('fs');
// const path = require('path');

// const logError = (error) => {
//   const logMessage = `[${new Date().toISOString()}] ${error.stack || error}\n`;
//   fs.appendFileSync(path.join(__dirname, 'backend_errors.log'), logMessage);
// };

// module.exports = logError;
